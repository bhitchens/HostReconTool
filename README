Fjer Host Analysis Tool

Dependencies:
Python 2.7
pywin32: https://pypi.python.org/pypi/pywin32
python WMI: https://pypi.python.org/pypi/WMI/
netaddr: https://pypi.python.org/pypi/netaddr

Run hostAnalysis.py with desired switches.

If run run without -r or --remote, the program will run the provided switches on the local system. If run with 
-r or --remote, ensure you run the command line with an account that has permission to run WMI on the remote 
system. The system data WMI command is run automatically.

-h or --help:           This help text
-d or --db:             Provide database name or full path to specify location
-o or --stout:          Send results to Standard Out
-i or --remote:         IP Address or CIDR-Notation range of IP Addresses. Exclude for Local Machine
-A or --all:            Run all switches
-u or --users:          User account data
-n or --netlogin:       Network Login data
-g or --groups:         Group data
-l or --ldisks:         Logical Disk data
-t or --timezone:       Timezone data
-s or --startup:        Startup Program data
-p or --profiles:       User Profiles data
-a or --adapters:       Netork Adapter data
-P or --process:        Processes data
-S or --services:       Services data
-r or --shares:         Shared Resources data
-D or --pdisks:         Physical Disk data
-m or --memory:         Physical Memory data